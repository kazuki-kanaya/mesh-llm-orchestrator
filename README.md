# Quegress

Quegress is a transparent queueing layer for asynchronous HTTP execution.
It accepts HTTP requests, queues them as jobs, and executes them through workers while preserving a familiar request/response interface.
